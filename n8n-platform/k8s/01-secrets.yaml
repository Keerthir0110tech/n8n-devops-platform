# NOTE: Do NOT commit real values to Git.
# In real production, replace this with External Secrets Operator / Sealed Secrets
# pulling from AWS Secrets Manager, GCP Secret Manager, or HashiCorp Vault.
# This file is a template — values below are placeholders to be replaced
# by your CI/CD pipeline or `kubectl create secret` at deploy time.
apiVersion: v1
kind: Secret
metadata:
  name: kubectl -n n8n-prod get secret n8n-secrets -o yaml
  namespace: n8n-prod
type: Opaque
stringData:
  postgres-password: "yourStrongPassword123"
  n8n-encryption-key: "someRandomKey"
